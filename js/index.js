const pickUserHand = (hand) => {
    console.log(hand);

    //Hide current page
    let hands = document.querySelector(".hands");
    hands.style.display = "none"
    //Show the choice on a page
    let choice = document.querySelector(".contest");

}